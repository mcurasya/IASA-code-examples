#pragma once //if file is added multiple times it will be added to executable file only once 

int assert_equals_int(int got, int expected);
int assert_equals_float(float got, float expected, float epsilon);